namespace: ${namespace}
stacker_bucket: ${stacker_bucket_name}

stacks:
  sampleapp:
    template_path: templates/bucket.json  # also supports yaml templates
    variables:
      ApplicationName: sampleapp
      CustomerName: ${customer}
      EnvironmentName: ${environment}
